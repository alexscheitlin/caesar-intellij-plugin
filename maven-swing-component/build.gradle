group 'ch.scheitlin.alex.maven'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {

    flatDir {
        dirs 'lib'
    }
}

dependencies {
    compile name: 'maven-model-1.0-SNAPSHOT'

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
